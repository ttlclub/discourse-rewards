{{#if siteSettings.discourse_rewards_enabled}}
  {{#if user.available_points}}
    {{d-icon "trophy"}}
    <h3><span class="desc">{{i18n "user.summary.total_earned_points" count=user.available_points}}</span>
      {{user.available_points}}</h3>
  {{/if}}
{{/if}}
